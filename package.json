{
	"name": "filterer",
	"description": "A dead simple filter chain for running multiple filters on an input value.",
	"repository" : {
		"type": "git",
		"url": "https://github.com/jameslnewell/js-filterer.git"
	},
	"devDependencies": {
		"mocha": "~1.18.2"
	},
	"scripts": {
		"test": "mocha -R spec ./test/test.js"
	}
}
